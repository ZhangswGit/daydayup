<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swapServer.mapper.ResourceMapper">

    <select id="findAllResourceByRoleId" resultMap="com.swapServer.mapper.CommonMapper.ResourceResultMap">
        select re.* from resource re
        left join role_resource rr
        on rr.resource_id = re.id
        left join role r
        on r.id = #{roleId}
        where rr.role_id = #{roleId} and re.is_delete = 0 and r.is_delete = 0;
    </select>
    
</mapper>
